import React from 'react';
import {
  StyleSheet,
  TouchableOpacity,
  View,
  TextInput,
  Text,
  Image, Alert, ImageBackground, Dimensions
} from 'react-native';

export default props => {
    
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text style={{fontSize:50}}>Noticias top aqui só noticia boa</Text>
        </View>
    )
}